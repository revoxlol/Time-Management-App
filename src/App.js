// Import necessary dependencies
import React from 'react';
import TimeManagementApp from './TimeManagementApp'; // Adjust the path based on your project structure

// Create the main App component
const App = () => {
  return (
    <div>
      {/* Other components or content can go here */}
      <TimeManagementApp />
    </div>
  );
};

// Export the App component
export default App;
